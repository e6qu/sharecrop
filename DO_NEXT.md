# Do Next

Prioritized queue:

1. Open and merge the reward bundles pull request from `task/reward-bundles`.
2. Then add remaining MCP workflow tools and full Streamable HTTP SSE, including reservation and approval tools, `GET /mcp`, `DELETE /mcp`, session enforcement, event IDs, and replay where practical.
3. Later: revisit collectible or inventory-based tips, anonymous workers, user/org-issued tokens, crypto reward metadata, and broader contract fixture coverage.

Before starting, reread [AGENTS.md](./AGENTS.md) and update the continuity files if task scope changes.
