# Do Next

Prioritized queue:

1. Open and merge the review outcomes pull request from `task/review-outcomes`.
2. Then replace single-kind reward handling with reward bundles containing credits, collectibles, both, or neither. Include collectible tips/inventory-based review extras in that design.
3. Then add remaining MCP workflow tools and full Streamable HTTP SSE, including reservation and approval tools, `GET /mcp`, `DELETE /mcp`, session enforcement, event IDs, and replay where practical.
4. Later: revisit anonymous workers, user/org-issued tokens, crypto reward metadata, and broader contract fixture coverage.

Before starting, reread [AGENTS.md](./AGENTS.md) and update the continuity files if task scope changes.
