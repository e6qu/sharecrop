name: Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: denoland/setup-deno@v2.0.4
        with:
          deno-version: v2.8.3
      - uses: actions/setup-go@v6.1.0
        with:
          go-version: "1.26.4"
      - name: Build demo WASM backend
        run: deno task wasm:demo:build
      - uses: actions/configure-pages@v6.0.0
      - uses: actions/upload-pages-artifact@v5.0.0
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v5.0.0
      - name: Check deployed routing
        run: deno task check:pages-routing -- --origin "${{ steps.deployment.outputs.page_url }}"
